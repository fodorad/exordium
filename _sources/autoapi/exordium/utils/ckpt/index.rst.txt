exordium.utils.ckpt
===================

.. py:module:: exordium.utils.ckpt

.. autoapi-nested-parse::

   Checkpoint utility functions.



Attributes
----------

.. autoapisummary::

   exordium.utils.ckpt.logger


Functions
---------

.. autoapisummary::

   exordium.utils.ckpt.build_hf_model
   exordium.utils.ckpt.download_file
   exordium.utils.ckpt.download_weight
   exordium.utils.ckpt.remove_token
   exordium.utils.ckpt.load_checkpoint


Module Contents
---------------

.. py:data:: logger

   Module-level logger.

.. py:function:: build_hf_model(model_cls: Any, model_id: str, pretrained: bool = True, config_cls: Any = None, **kwargs: Any) -> Any

   Build a HuggingFace model, with or without its pretrained weights.

   With ``pretrained=False`` only the model's ``config.json`` is fetched (kilobytes)
   and the architecture is instantiated with **random weights** — no checkpoint is
   downloaded.  The result has the same shapes and interface as the real model, so it
   is enough to exercise input/output contracts, inspect the architecture, or work
   offline; its *predictions are meaningless*.

   For reference, a CLIP ViT-H/14 costs ~15 KB this way instead of ~3.7 GB.

   :param model_cls: The HuggingFace model class (e.g. ``CLIPVisionModelWithProjection``).
   :param model_id: HuggingFace Hub model identifier.
   :param pretrained: ``True`` (default) downloads the real weights. ``False`` builds the
                      architecture with random weights.
   :param config_cls: Config class to load with. Defaults to ``AutoConfig``; pass a
                      specific one when the model needs a sub-config (e.g. ``CLIPVisionConfig``
                      for a vision tower inside a full CLIP checkpoint).
   :param \*\*kwargs: Forwarded to ``from_pretrained`` when *pretrained* is ``True``.

   :returns: The instantiated model. Typed as ``Any`` because the concrete class comes from
             *model_cls*, mirroring ``from_pretrained``'s own dynamic return.


.. py:function:: download_file(remote_path: str, local_path: pathlib.Path | str, overwrite: bool = False) -> None

   Downloads a file to a given local path using urllib.

   :param remote_path: URL of the remote file.
   :type remote_path: str
   :param local_path: Destination path for the downloaded file.
   :type local_path: Path | str
   :param overwrite: If True, re-downloads even if the file
                     already exists. Defaults to False.
   :type overwrite: bool, optional

   :raises FileNotFoundError: If the file is not present after downloading.


.. py:function:: download_weight(filename: str, local_dir: pathlib.Path | str, repo_id: str = _HF_REPO_ID, upstream_repo_id: str | None = None, upstream_filename: str | None = None, upstream_url: str | None = None) -> pathlib.Path

   Download a model weight file from Hugging Face Hub if not already cached.

   Fetches *filename* from :data:`_HF_REPO_ID` — our own mirror — and places it in
   *local_dir*.  Subsequent calls with the same arguments are no-ops.

   The mirror is preferred because upstream sources move or vanish (the 6DRepNet
   author's original link already did).  When an upstream is given it is used as a
   **fallback**: if the mirror is unreachable or missing the file, the original source
   is tried before giving up, so neither location is a single point of failure.

   The fallback may be another Hub repo (*upstream_repo_id*) or a plain URL
   (*upstream_url*), since not every upstream publishes to the Hub — EmotiEffLib, for
   instance, serves its checkpoints straight from GitHub.

   :param filename: Name of the file in *repo_id* (e.g. ``"fabnet_weights.pth"``).
   :param local_dir: Directory to store the file in; it lands at ``local_dir / filename``.
   :param repo_id: Hub repo to fetch from. Defaults to our mirror,
                   ``"fodorad/exordium-weights"``.
   :param upstream_repo_id: Original Hub repo, used only if *repo_id* fails.
   :param upstream_filename: Filename within *upstream_repo_id*, if it differs from
                             *filename*. Defaults to *filename*.
   :param upstream_url: Original download URL, used only if the Hub sources fail.

   :returns: Path to the downloaded (or already cached) local file.

   :raises FileNotFoundError: If the file is missing after every download attempt.


.. py:function:: remove_token(weights: dict, token: str = '_model.') -> dict

   Removes a token from the keys of the weight dictionary.

   The ckpt file saves the pytorch_lightning module which includes its child
   members.  Loading the state_dict with the child called ``_model.`` leads
   to an error; removing the unnecessary token fixes checkpoint loading.

   :param weights: Loaded torch weights.
   :type weights: dict
   :param token: String to be removed. Defaults to ``"_model."``.
   :type token: str, optional

   :returns: Updated weight dictionary.
   :rtype: dict


.. py:function:: load_checkpoint(path: pathlib.Path | str, strip_prefix: str = 'module.') -> dict

   Load a torch checkpoint and strip DDP/Lightning key prefixes.

   Handles both raw ``state_dict`` files and Lightning checkpoint files that
   store the state dict under a ``"state_dict"`` key.  Keys starting with
   ``strip_prefix`` have that prefix removed so the dict can be passed
   directly to ``model.load_state_dict()``.

   :param path: Path to the ``.pth`` or ``.ckpt`` file.
   :type path: Path | str
   :param strip_prefix: Prefix to remove from each key.
                        Defaults to ``"module."`` (DDP convention).
   :type strip_prefix: str, optional

   :returns: Cleaned state dict ready for ``model.load_state_dict()``.
   :rtype: dict


